#include <iostream>   // Thư viện nhập xuất cơ bản (cin, cout)
#include <cmath>      // Thư viện toán học (căn bậc 2 sqrt, lũy thừa pow, trị tuyệt đối abs)
#include <string>     // Thư viện xử lý chuỗi ký tự (string)
#include <algorithm>  // Thư viện thuật toán có sẵn (tìm max, min, sắp xếp sort)
#include <vector>     // Thư viện mảng động (học ở phần nâng cao)

using namespace std;

// =========================================================================
// KHU VỰC 1: NƠI ĐỊNH NGHĨA HÀM (FUNCTIONS) - Viết hàm của bạn ở đây (nếu có)
// =========================================================================



// =========================================================================

int main() {
    // 1. Tự động sửa lỗi hiển thị font Tiếng Việt trên Terminal Windows
    system("chcp 65001 > nul"); 

    // 2. Thần chú tăng tốc độ nhập xuất dữ liệu (Đi thi cực kỳ cần để không bị quá thời gian - Time Limit Exceeded)
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // =====================================================================
    // KHU VỰC 2: VIẾT CODE CHÍNH CỦA BẠN VÀO ĐÂY
    
    cout << "Bộ khung toàn năng QHQ04 đã kích hoạt thành công! Chiến thôi bro! 🚀\n";



    // =====================================================================

    return 0; // Câu lệnh kết thúc chương trình bắt buộc
}